import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    alertContainer: {
        padding: 12,
        borderRadius: 8,
        borderWidth: 1,
        marginBottom: 8,
        width: '100%',
    },
    alertText: {
        fontSize: 13,
        fontWeight: '600',
        textAlign: 'center',
    },
});
